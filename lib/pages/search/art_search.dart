import 'package:flutter/material.dart';

class ArtSearchPage extends StatelessWidget {
  const ArtSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("작품 검색 결과 페이지"),
      ),
    );
  }
}
