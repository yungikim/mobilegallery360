import 'package:flutter/material.dart';

class ArtistSearchPage extends StatelessWidget {
  const ArtistSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("작가 검색 결과 페이지"),
      ),
    );
  }
}
