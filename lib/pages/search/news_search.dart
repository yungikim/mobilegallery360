import 'package:flutter/material.dart';

class NewSearchPage extends StatelessWidget {
  const NewSearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("News 검새 결과 페이지"),
      ),
    );
  }
}
